services:
  - type: web
    name: zunish-riff-generator-web
    env: python
    plan: free
    buildCommand: pip install -e .
    startCommand: uvicorn zunish.server:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.13
