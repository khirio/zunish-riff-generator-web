# 生成ルール

`zunish-riff-generator` が実際に使っている音楽理論上のルールを、コードの内容に基づいて整理したものです。要素そのもの（進行・リズム・スケール・リフ・伴奏パターン）は `src/zunish/content/` 配下のレジストリで管理されており、このドキュメントはその中身と、それらを組み合わせる `generator.py` / `director.py` のロジックを説明します。

## 調とコード表記の前提（`theory.py`）

- 曲の調は「短調のトニックのピッチクラス（0〜11）」として保持します。
- コードのローマ数字表記（`IV`, `V`, `VIm`, `Vsus4` など）は、その短調の**平行長調**（短調トニック+3半音）を基準に書かれています。例えば `VIm` は平行長調の第VI度＝短調トニックそのものを指します。
- 度数のあとに `#` / `b` を置くと半音上げ／下げできます（例：`V#dim` は第V度を半音上げた音をルートとするdimトライアド）。
- 対応するコード品質（三和音）は以下の4種類のみ：
  - `""`（メジャートライアド）: 0, 4, 7
  - `"m"`（マイナートライアド）: 0, 3, 7
  - `"sus4"`（sus4トライアド）: 0, 5, 7
  - `"dim"`（ディミニッシュトライアド）: 0, 3, 6

## コード進行（`content/progressions.py`）

各進行はローマ数字のシーケンスと、次にどの進行へ遷移しやすいか（`follows` の重み）を持つ有向グラフになっています。`VI`（6）と`IV`（4）は見間違えやすいため、各進行にアラビア数字表記（度数のみ。マイナーは`m`、sus4/dimはそのまま、`#`は半音上げを表す）を併記しています。登録されている進行：

| ID | 進行 | アラビア数字表記 | 開始重み |
|---|---|---|---|
| `iv_v_vim` | IV - V - VIm | 4-5-6m | 2.0 |
| `vim_v_iv` | VIm - V - IV | 6m-5-4 | 1.0 |
| `vim_v_ii` | VIm - V - II | 6m-5-2 | 0.4 |
| `vim_iiim_ii` | VIm - IIIm - II | 6m-3m-2 | 0.4 |
| `vim_v_iv_v` | VIm - V - IV - V（Vターン） | 6m-5-4-5 | 0.4 |
| `iv_v_vi_picardy` | IV - V - VI（ピカルディ終止） | 4-5-6 | 1.0 |
| `iv_v_vsus4_vi` | IV - V - VIsus4 - VI | 4-5-6sus4-6 | 1.0 |
| `oudou_tension_nuki` | IV - V - III - VIm（王道進行テンション抜き） | 4-5-3-6m | 1.0 |
| `jado_shinkou` | IV - V - V#dim - VIm（邪道進行） | 4-5-5#dim-6m | 1.0 |
| `shidan_nagashi` | VIm - V - IV - III（四段流し） | 6m-5-4-3 | 1.0 |
| `avalanche_full` | VIm - III - V - II - IV - I - IIm - III（雪崩クリシェ） | 6m-3-5-2-4-1-2m-3 | 1.0 |
| `avalanche_half` | VIm - III - V - II（雪崩クリシェ前半） | 6m-3-5-2 | 1.0 |
| `iv_v_vim_2bar` | IV(2分)-V(2分)-VIm（全音符）（2コード/小節） | 4-5-6m | 2.0 |
| `vim_v_iv_2bar` | VIm(2分)-V(2分)-IV（全音符）（2コード/小節） | 6m-5-4 | 1.0 |
| `vim_v_ii_2bar` | VIm(2分)-V(2分)-II（全音符）（2コード/小節） | 6m-5-2 | 0.4 |
| `vim_iiim_ii_2bar` | VIm(2分)-IIIm(2分)-II（全音符）（2コード/小節） | 6m-3m-2 | 0.4 |
| `iv_v_vi_picardy_2bar` | IV(2分)-V(2分)-VI（全音符、ピカルディ終止）（2コード/小節） | 4-5-6 | 1.0 |

`follows` の重みは「今の進行が終わったあと、次にどの進行を選ぶか」を決める確率分布です（例：`iv_v_vim` は自身への回帰が1.0、`shidan_nagashi`へ1.4など）。最頻パターンである `iv_v_vim`/`iv_v_vim_2bar` は、自身（および互いの2コード/小節版）への遷移が突出しすぎないよう、`shidan_nagashi` や `oudou_tension_nuki` など他の進行にも適度な重みを配分している。`follows` が空、または遷移先が見つからない場合は、全進行を`start_weight`で重み付けした中から選び直します。`avalanche_half`（雪崩クリシェ前半）は `follows` を `iv_v_vim` / `iv_v_vi_picardy` / `iv_v_vsus4_vi` の3つだけに絞ってあり、必ずこのいずれかへ遷移する（`director.py` は `follows` が空でない限りそこから選ぶだけでフォールバックしないため）。

各コードには小節内での音価（`beats`）があり、デフォルトは全音符（1コード=1小節）。3コードの進行5つ（`iv_v_vim`, `vim_v_iv`, `vim_v_ii`, `vim_iiim_ii`, `iv_v_vi_picardy`）には、1・2番目のコードを2分音符、3番目を全音符とする「2コード/小節」の姉妹進行（`iv_v_vim_2bar` など）が追加で登録されており、元の進行の `follows` からランダムに遷移してくる。2分音符のコードは常に同じ進行内の隣接コードとペアになり、1小節分を完結させる（`validate()` がこれを保証する）。

## 進行の連結・転調ロジック（`director.py`）

- 単一の進行を **2〜4回（ランダム）ループ** してから次の進行へ移ります（`MIN_PROGRESSION_REPEATS=2`, `MAX_PROGRESSION_REPEATS=4`）。
- 進行を切り替えるたびに **30%の確率で転調**（`MODULATION_PROBABILITY=0.3`）。転調幅は常に **短3度（±3半音）**（`MODULATION_SEMITONES=3`）で、上下どちらに動くかはランダムです。
- セクション分けはなく、無限に連続する単一の進行ストリームとして生成されます。
- コード列は拍数（`beats`）が`BEATS_PER_BAR`（4拍）に達するたびに1小節としてグループ化される。1小節に収まるコードは1つ（全音符）または2つ（2分音符×2）。

## スケール（`content/scales.py` / `theory.py`）

メロディの非コードトーン（経過音・刺繍音）を選ぶ際に使うスケールで、短調トニックからの半音オフセットで定義されています。

| ID | 名前 | 度数オフセット | 重み |
|---|---|---|---|
| `minor_pentatonic_2_6nuki` | 短調2・6抜き（ペンタトニック） | 0, 3, 5, 7, 10 | 1.2 |
| `minor_6nuki` | 短調6抜き | 0, 2, 3, 5, 7, 10 | 1.5 |
| `harmonic_minor` | 和声的短音階（ZUN流） | 0, 2, 3, 5, 7, 10, 11 | 1.0 |
| `dorian_6` | ドリアの6度（ナチュラルマイナーの6度を上げたもの） | 0, 2, 3, 5, 7, 9, 10 | 1.0 |

小節ごとに上記スケールを重み付き抽選し、そのバーの装飾音（非コードトーン）の候補プールを決めます。

※ `harmonic_minor` は教科書的な和声的短音階（b6・maj7）ではなく、b6を欠きb7とmaj7を両方含む形（ZUN曲で頻出する用法）を採用しています。

## 著名リフ・アルペジオ（`content/riffs.py`）

コードのクオリティ（`compatible_qualities`）に応じて発動する固定モチーフです。自身のルート音からの半音オフセットで保存され、そのバーのコードのルート／オクターブに移調して使われます。

| ID | 名前 | 対応コード品質 | 音形（ルートからの半音） |
|---|---|---|---|
| `sus4_seventh_arpeggio` | 7sus4アルペジオ | `sus4` | 0, 5, 7, 10, 7, 5, 0, 5（A3-D4-E4-G4-E4-D4-A3-D4 相当） |
| `alice_arpeggio` | アリスアルペジオ | `m` | 0, 7, 5, 7, 3, 7, 2, 7（A3-E4-D4-E4-C4-E4-B3-E4 相当） |

右手パートを生成する際、そのバーのコード品質に合致するリフがあれば重み付き抽選の対象に加わり、「リフを使わない（＝下記のスケールウォークで生成する）」という選択肢も同じ重み合計で候補に入ります。リフが選ばれた場合は各音符を8分音符間隔で並べ、最後の音符だけ小節末まで伸ばします。

1小節に2コードある場合、リフの発動判定・ルート/オクターブは小節内の**先頭コード**のみを見る。リフが発動したときは、2つ目のコードの有無にかかわらず、これまでと同じ固定の長さ（8分音符×8＝1小節分）でそのまま鳴らす。

## リズムパターン（`content/rhythms.py`）

リフが選ばれなかった場合の右手メロディ（スケールウォーク）に使うリズムです。1小節＝16分音符16個分の長さの並びとして定義されています。

| ID | 名前 | 16分音符単位の並び | 重み |
|---|---|---|---|
| `tresillo_16` | Tresillo | 6, 6, 4（付点8分+付点8分+8分） | 2.0 |
| `straight_sixteenths` | 均等16分 | 1 × 16 | 1.0 |
| `sixteenth_pickup` | 16分ピックアップ | 1, 1, 2, 4, 2, 2, 2, 2 | 2.0 |
| `gallop_16th` | ギャロップ | 1, 1, 2, 1, 1, 2, 4, 4 | 1.5 |

## 右手メロディ生成（スケールウォーク時、`generator.py`）

リフが選ばれなかった小節では、以下のルールで右手メロディを生成します。

- **禁則音（b6）**: 短調トニック+8半音（Aマイナーの場合F）は、コードトーンであってもメロディには一切使いません（`BANNED_DEGREE_OFFSET=8`）。
- **ドリアの上げ6度**: 短調トニック+9半音（Aマイナーの場合F#）は、装飾音プールからは常に除外され、そのバーのコードトーンと一致する場合のみメロディに現れます（`DORIAN_SIXTH_OFFSET=9`）。
- **和声的短音階の導音（上主音）**: 短調トニック+11半音（Aマイナーの場合G#）が装飾音（非コードトーン）としてメロディに現れる場合は、必ず0の音（トニック）へ上行解決する装飾音としてのみ使われ、それ以外の音（例: 1半音下の♭7度）への隣接装飾音としては選ばれません（`LEADING_TONE_OFFSET=11`）。ただし、そのバーのコード自身がこの音をコードトーンとして含む場合（例: `III`の3rd、`V#dim`のroot）は、装飾音プールを経由せず通常のコードトーンとしてメロディに現れるため、この制約は適用されず自由に他の音へ遷移できます。
- コードトーン（ルート/3rd/5th、禁則音を除く）をC4=60〜A5=81のMIDI範囲でプール化し、ランダムな開始インデックスから**歩行**：各拍で -2/-1/+1/+2 ステップ（重み1:3:3:1、隣接ステップに偏重）だけプール内インデックスを動かして次の音を選びます。
- **アポジャトゥーラ（倚音）**: 拍頭（強拍）にあり、かつ音価が8分音符以上の音は、50%の確率（`APPOGGIATURA_PROBABILITY`）で直前に全音以内（`NEIGHBOR_TONE_MAX_STEP=2`半音）の装飾音（非コードトーン）を差し込み、元の音と半分ずつの長さに分割します。1小節あたり最大2回まで（`MAX_NON_CHORD_TONES_PER_BAR=2`）。
- **ダイアド（重音）**: 音価が8分音符以上の音は、35%の確率（`DYAD_PROBABILITY`）で別のピッチクラスのコードトーン（最も近い上下どちらか）を重ねて響かせます。
- **1小節に2コードある場合**: リズムパターン・スケールの抽選は小節全体で1回のみ（コードの境目を考慮しない）。各拍のコードトーン・装飾音プールは、その拍がどちらのコード区間に属するかで切り替わる。コードが変わった直後は、直前のピッチに最も近い新コードの音へ滑らかに乗り継いでウォークを続けるため、メロディがコードの境目を自然に跨ぐ。
- **前乗りシンコペーション**: 著名リフが選ばれた小節を除き、1/3の確率（`RIGHT_HAND_ANTICIPATION_PROBABILITY`）で小節最後の8分音符を次の小節の先頭コードへ前乗りさせる。直前のメロディ音に最も近い次コードの構成音（禁則音のb6を除く）へピッチを乗せ替え、4分音符（8分+8分相当、小節線をまたいで次小節の頭まで）として鳴らす。

## 左手伴奏パターン（`content/accompaniment.py` / `generator.py`）

小節ごとに重み付き抽選で選ばれます。左手のノートは常に現在のコード区間の範囲内で完結する。1小節に2コードある場合は、各コード区間ごとに伴奏パターン（block/broken）とボイシングを個別に抽選し、その区間の拍数（2拍または4拍）に比例縮小して演奏する（例: 通常8分音符のパターンは2拍区間では16分音符相当に圧縮される）。次の小節の和音を先取りする（シンコペーションする）ことはない（右手側は上記の「前乗りシンコペーション」として実装済み）。

| ID | 名前 | 種別 | 内容 |
|---|---|---|---|
| `block_chord` | ブロックコード | `block` | root-3rd-5thをそのコード区間いっぱいに同時に鳴らす（1小節に1コードなら4拍、2コードなら各2拍） |
| `broken_root_fifth_third_fifth` | ブロークン(root-5th-3rd-5th) | `broken` | root→5th→3rd→5th→root→5th→3rd→5th を8分音符8つで演奏 |
| `dotted_walk` | 付点2.5拍→16分ウォーク→8分 | `broken` | root(2.5拍)→root-3rd-5th-rootの16分ウォーク(1拍)→root(8分, 0.5拍) |
| `tresillo_ornamented` | Tresillo複合(16分/32分装飾) | `broken` | 32分の装飾＋sustain(1.5拍)→sustain(1.5拍)→root-3rd-5th-rootの16分ウォーク(1拍) |

- `block`種はコードの3音すべてをそのコード区間いっぱい（1小節に1コードなら4拍、2コードなら各2拍）伸ばします。ボイシングは毎回 `content/voicings.py` から重み付き抽選され、基本形だけでなく第一転回形・第二転回形・オープン（中声部オクターブ上げ）も混ざります（下記参照）。
- `broken`種は `note_order`（コード内の音のインデックス、0=root/1=3rd/2=5th。`None`はレスト＝無音で時間だけ進める）と `thirty_second_note_durations`（各音の長さ・32分音符単位、1小節=32として定義。2拍のコード区間で使う場合は上記の通り比例縮小される）に従って順に演奏します。
- 左手はオクターブ2、ベース音量78（右手のベース音量100より小さい）にジッター±5を加えて発音します。

## 左手ブロックコードのボイシング多様化（`content/voicings.py` / `generator.py`）

ブロックコード（`kind == "block"`）を鳴らすたびに、以下から重み付き抽選で1つ選ばれます。オクターブ2の基本形だけだと低音域に音が集まって濁って聞こえるため、ベース音を変えたりオープンにしたりして響きを分散させます。

| ID | 名前 | 内容 |
|---|---|---|
| `root_position` | 基本形 | root-3rd-5th（変更なし） |
| `first_inversion` | 第一転回形 | 3rd-5th-root（rootを1オクターブ上げてベースを3rdに） |
| `second_inversion` | 第二転回形 | 5th-root-3rd（root・3rdを1オクターブ上げてベースを5thに） |
| `open_middle_octave_up` | オープン(中声部オクターブ上げ) | root-5th-3rd（基本形の中声部=3rdだけ1オクターブ上げる） |

## 要素の追加方法

`src/zunish/content/` 配下の該当ファイルに `_register(...)` 呼び出しを1つ追加するだけで、コアロジック（`generator.py` / `director.py`）を変更せずに新しい要素を組み込めます。詳細は [README.md](README.md#要素の追加方法) を参照してください。
